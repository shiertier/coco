# Releases

<PostList
  currentPage={props.currentPage}
  pages={props.pages.filter(page => page.name.includes('react-aria') && page.name.includes('releases') && !page.name.includes('index')) ?? []}
/>

For all previous releases, see the [Archived releases](s2:v3/releases/index.md) page.

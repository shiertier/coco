# Releases

<PostList
  currentPage={props.currentPage}
  pages={props.pages.filter(page => page.name.includes('s2') && page.name.includes('releases') && !page.name.includes('index')) ?? []}
/>

For all previous releases of React Spectrum v3, see the [Archived releases](../v3/releases/.md) page.
